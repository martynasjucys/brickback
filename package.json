{
  "name": "brickback",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev --filter @brickback/web",
    "web": "pnpm --filter @brickback/web dev",
    "db:types": "supabase gen types typescript --project-id nthbhcqufiuyrnioxglm > packages/shared/src/db/types.ts"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5",
    "prettier": "^3"
  }
}
